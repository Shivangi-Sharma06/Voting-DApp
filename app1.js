let provider;
let signer;
let UserAddress;
let contract;
let account;

async function connect(){
    if (type of window.ethereum !== 'undefined'){
        try{
            console.log("requesting account access");
            const accounts=await window.ethereum.request({method: })
        }
    }
}